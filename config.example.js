// Configuration Template
// Copy this file to config.js and fill in your values
// Then update app.js to import from config.js instead of hardcoding

const CONFIG = {
    SUPABASE_URL: 'YOUR_SUPABASE_URL',  // From Supabase Settings > API > Project URL
    SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'  // From Supabase Settings > API > anon/public key
};

// If using as a module (optional):
// export default CONFIG;

